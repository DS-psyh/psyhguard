<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Личный кабинет — PsyhGuard</title>
  <style>
    :root{
      --bg:#f7f8fb; --card:#ffffff; --txt:#1f2937; --muted:#6b7280; --line:#e8ebf2;
      --accent:#3b82f6;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter,ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto;background:var(--bg);color:var(--txt)}
    .app-header{
      position:sticky;top:0;z-index:100;display:flex;justify-content:space-between;align-items:center;
      padding:10px 16px;background:rgba(255,255,255,.85);backdrop-filter:blur(6px);border-bottom:1px solid var(--line)
    }
    .cabinet-link{
      display:inline-flex;align-items:center;gap:.5rem;padding:8px 14px;border-radius:9999px;background:#fff;border:1px solid var(--line);
      font-weight:600;text-decoration:none;color:var(--txt);box-shadow:0 1px 2px rgba(0,0,0,.04)
    }
    .cabinet-link:hover{background:var(--accent);color:#fff}
    .wrap{max-width:960px;margin:24px auto 96px;padding:0 14px}
    h2{margin:0 0 12px}
    .section{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:16px;margin-top:16px;box-shadow:0 2px 8px rgba(17,24,39,.04)}
    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:10px}
    .chip{
      display:flex;align-items:center;justify-content:center;text-align:center;
      padding:12px 14px;border-radius:16px;background:#fff;border:1px solid var(--line);font-weight:600
    }
    .muted{color:var(--muted)}
  </style>
</head>
<body>
  <header class="app-header">
    <div class="muted">Личный кабинет</div>
    <a class="cabinet-link" href="index.html">← Вернуться в приложение</a>
  </header>

  <main class="wrap">
    <section class="section">
      <h2>История вопросов</h2>
      <div id="history" class="grid"></div>
      <div id="empty" class="muted" style="display:none;margin-top:8px;">Пока пусто. Задайте вопрос в приложении — он появится здесь.</div>
    </section>

    <section class="section">
      <h2>Приложения</h2>
      <div class="grid">
        <a class="chip" href="index.html">Аппроксимации</a>
        <!-- Добавь сюда ссылки на другие инструменты по мере появления -->
        <a class="chip" href="#" onclick="alert('Заглушка другого приложения');return false;">Инструмент 2</a>
      </div>
    </section>
  </main>

  <script>
    // Показ истории из localStorage (используется тот же ключ, что и в приложении)
    const STORAGE_KEY="psyhguard-pwa-v2";
    function loadAll(){ try{const x=localStorage.getItem(STORAGE_KEY);return x?JSON.parse(x):[]}catch{return[]} }

    const list=loadAll();
    const box=document.getElementById('history');
    const empty=document.getElementById('empty');

    if(!list.length){ empty.style.display='block'; }
    else{
      list.forEach(p=>{
        const div=document.createElement('div');
        div.className='chip';
        div.textContent = (p.question||'Без вопроса');
        box.appendChild(div);
      });
    }
  </script>
</body>
</html>
